# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
.expo-shared/
.expo-bundle-check/
.expo-export-test/
.expo-export-test2/
.expo-test-bundle/
.expo-test-bundle2/
.expo-test-bundle3/
.expo-tmp-export/
dist/
dist-*/
web-build/
build/
coverage/
expo-env.d.ts

# Native / local prebuild output (regenerate with `npx expo prebuild` when needed)
.kotlin/
*.orig.*
/ios
/android
android/
android_old/
ios/
ios/Pods/
ios/build/
android/.gradle/
android/app/build/
android/build/

# Metro / Gradle local caches
.metro-health-check*
.metro-tmp/
.metro-temp/
.metro-cache/
.metro-export-log.txt
.gradle-user-home/
node_modules/.cache/

# debug / logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
*.log.gz
debug-aar-error.txt
assemble-debug-full.txt
gradle-local.log
eas-build*.log*
eas-build-*.tar.gz
eas-decoded.log
eas-build-view*.json
rn-config.err
rn-config.json
.tmp-*
.tmp-*/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# local env files (never commit secrets)
.env
.env.*
  !.env.example
  !.env.local.example
  !.env.qa.example
  !.env.production

# QA Test Center results (regenerated locally)
test-results/

# local export / zip artifacts
.expo-export-test/
.expo-export-test2/
sitemap-001.xml.gz
*.zip
Bhuguard-dev-build-scripts.zip
bhuguard-mobile.zip

# typescript
*.tsbuildinfo

# Build outputs
releases/
*.apk
*.aab
*.ipa

# Signing / certificates / private keys
*.jks
*.keystore
*.p12
*.p8
*.pem
*.key
*.mobileprovision

# Firebase / Google services (transfer privately if required)
google-services.json
GoogleService-Info.plist

# Local backup / sync staging folders (not source of truth)
_migration_backup_*/
_ubuntu_sync_stage/
.claude/

# Local one-off backup copies created during fixes
*.before-*
*.backup
app.config.js.backup
app.json.backup
package.json.backup
