/**
 * Mobile login route entry — premium glass LoginScreen.
 * Kept as a thin re-export so RootNavigator stays stable.
 */
export { LoginScreen as MobileLoginScreen } from '../../components/auth/login/LoginScreen';
